# --- Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Configuration ---
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# --- Milvus ---
pymilvus>=2.4.0

# --- Document Parsing ---
unstructured[all-docs]>=0.13.0
markdown>=3.6
python-magic>=0.4.27; sys_platform != 'win32'
python-magic-bin>=0.4.14; sys_platform == 'win32'

# --- Document Parsing Fallback (Pure Python) ---
PyMuPDF>=1.23.0          # PDF fallback
python-docx>=1.1.0       # DOCX fallback
openpyxl>=3.1.0          # XLSX fallback
python-pptx>=0.6.21      # PPTX fallback
ebooklib>=0.18           # EPUB fallback
beautifulsoup4>=4.12.0   # HTML/EPUB text extraction

# --- Text Processing & Chunking ---
langchain>=0.1.16
langchain-community>=0.0.34
llama-index>=0.10.30
llama-index-core>=0.10.30
llama-index-embeddings-ollama>=0.1.0
sentence-transformers>=2.6.0
transformers>=4.39.0
tokenizers>=0.15.0

# --- BM25 / Hybrid Retrieval ---
rank-bm25>=0.2.2
jieba>=0.42.1

# --- Reranker ---
FlagEmbedding>=1.2.11

# --- Auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Database ---
asyncpg>=0.29.0

# --- Async Tasks ---
celery[redis]>=5.3.0
redis>=5.0.0

# --- HTTP Client ---
httpx>=0.27.0
aiohttp>=3.9.0
requests>=2.31.0

# --- Utilities ---
numpy>=1.26.0
tqdm>=4.66.0
uuid6>=2024.1.12
