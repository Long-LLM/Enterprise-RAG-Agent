# 企业级 RAG 系统工程化落地全流程指南

> 基于 FastAPI + Milvus + Ollama + Vue 3 + Celery 的私有化部署实践
>
> 配套开源项目：Enterprise RAG Agent（支持 7 种分块策略、混合检索、异步处理、三角色权限）

---

## 目录

1. [需求分析与场景定义](#一需求分析与场景定义)
2. [技术选型决策](#二技术选型决策)
3. [架构设计](#三架构设计)
4. [核心模块实现](#四核心模块实现)
5. [开发规范与工程实践](#五开发规范与工程实践)
6. [权限与安全](#六权限与安全)
7. [可观测性与运维](#七可观测性与运维)
8. [部署方案](#八部署方案)
9. [常见问题与排障](#九常见问题与排障)
10. [性能调优](#十性能调优)
11. [上线检查清单](#十一上线检查清单)
12. [总结与延伸](#十二总结与延伸)

---

## 一、需求分析与场景定义

### 1.1 明确业务场景

在动手之前，先回答三个问题：

| 问题 | 示例 |
|------|------|
| **谁在用？** | 内部员工（按部门隔离）、外部客户（公共问答） |
| **问什么？** | 产品手册查询、规章制度问答、技术文档检索 |
| **数据从哪来？** | PDF 产品手册、Word 制度文件、Excel 参数表、内部 Wiki |

### 1.2 权限模型设计

企业场景必须考虑**数据隔离**，参考本项目的三角色模型：

- **Admin**：可见所有文档，可分配用户部门，可设置公共文档，可管理用户
- **Staff**：可见公共文档 + 本部门文档，可上传/管理本部门文档
- **User**：仅可见公共文档，只能查询不能上传

**实践建议**：
- 文档标记 `department` 字段，为空表示公共文档
- 用户标记 `role` + `department`，权限按角色 + 部门双重校验
- 公共文档用特殊机制（`__public__` 用户权限表）统一管控
- 权限过滤必须落到**检索层**（向量库的 `doc_id in [...]` 过滤），不能只在 API 层做

### 1.3 关键非功能性需求

| 需求维度 | 目标值 | 实现手段 |
|---------|--------|---------|
| 首字节延迟 | < 1s | 流式 SSE + 意图判断快速路径 |
| 文档处理 | 100 页 PDF < 30s | Celery 异步 + GPU Embedding |
| 检索准确率 | top-5 命中率 > 85% | 混合检索 + 重排 + 查询改写 |
| 并发支持 | 50 QPS | 异步 IO + 连接池 + 容器化 |
| 数据安全 | 完全私有化 | Ollama 本地推理 + JWT 鉴权 |

---

## 二、技术选型决策

### 2.1 核心组件对比

| 组件 | 选型 | 备选 | 选择理由 |
|------|------|------|---------|
| **后端框架** | FastAPI | Django/Flask | 原生异步、自动生成 OpenAPI、Pydantic 校验 |
| **向量数据库** | Milvus 2.4+ | Qdrant/Pinecone | 国产友好、分布式扩展、Attu 可视化、IVF_FLAT 性能可控 |
| **元数据库** | PostgreSQL 16 (生产) / SQLite (开发) | MySQL | asyncpg 高性能、`METADATA_DB_URL` 一行切换 |
| **Embedding** | Ollama + bge-m3 (1024 维) | OpenAI API | 完全私有化、中英混合优化、无数据出境 |
| **LLM** | Ollama + qwen2.5:7b-instruct | GPT-4 / vLLM | 本地部署、中文优化、零 API 费用 |
| **Reranker** | BGE-Reranker (FlagEmbedding) | Cohere Rerank | 开源免费、与 bge-m3 同源 |
| **任务队列** | Celery + Redis 7 | RQ / Dramatiq | 成熟稳定、支持重试和进度回调 |
| **前端** | Vue 3 + Element Plus + Vite | React/Ant Design | 轻量、中文组件库完善、Vite 启动快 |

### 2.2 私有化 vs 云服务

| 维度 | 私有化 (Ollama) | 云服务 (OpenAI) |
|------|----------------|----------------|
| 数据安全 | 完全不出境 | 数据经过第三方 |
| 成本 | 一次性 GPU 投入 | 按 token 计费，量大昂贵 |
| 延迟 | 局域网内 <100ms | 网络依赖，100-500ms |
| 维护 | 需要自己运维模型 | 零运维 |
| 灵活性 | 可换任意开源模型 | 受限于供应商模型 |

**企业落地建议**：
- 涉密场景必须私有化
- 可先私有化跑通流程，再评估是否引入云端增强模型（项目预留 `LLM_PROVIDER=vllm` 切换能力）
- GPU 推荐：RTX 4090 / A100，16GB 显存可跑 7B INT4 量化模型

---

## 三、架构设计

### 3.1 整体架构

```
                                ┌─────────────────────┐
                                │   前端层 (Vue 3)     │
                                │  - 智能问答 (SSE)    │
                                │  - 文档上传/管理      │
                                │  - 权限与会话管理     │
                                └──────────┬──────────┘
                                           │ HTTPS + JWT
                                           ▼
                                ┌─────────────────────┐
                                │  API 网关 (Nginx)    │
                                └──────────┬──────────┘
                                           │
                                           ▼
                              ┌──────────────────────────┐
                              │   FastAPI 后端           │
                              │   ┌────────────────────┐  │
                              │   │ DI Container       │  │
                              │   │ (服务生命周期管理)  │  │
                              │   └────────────────────┘  │
                              │   ┌────────────────────┐  │
                              │   │ Service 层         │  │
                              │   │ - RAGService       │  │
                              │   │ - DocumentService  │  │
                              │   └────────────────────┘  │
                              │   ┌────────────────────┐  │
                              │   │ Core 层            │  │
                              │   │ - Parser/Chunking  │  │
                              │   │ - Embedding/LLM    │  │
                              │   │ - Retrieval/Rerank │  │
                              │   └────────────────────┘  │
                              └─────┬──────┬──────┬───────┘
                                    │      │      │
                ┌───────────────────┘      │      └────────────────────┐
                ▼                          ▼                            ▼
        ┌──────────────┐         ┌────────────────┐           ┌─────────────────┐
        │   Milvus     │         │  PostgreSQL    │           │ Celery Worker   │
        │  向量存储    │         │  元数据存储     │           │ + Redis Broker  │
        │ IVF_FLAT     │         │ 用户/会话/权限  │           │ 异步文档处理     │
        │ COSINE       │         │                │           │                 │
        └──────────────┘         └────────────────┘           └────────┬────────┘
                                                                       │
                                                                       ▼
                                                              ┌─────────────────┐
                                                              │     Ollama      │
                                                              │  bge-m3 (Embed) │
                                                              │  qwen2.5 (LLM)  │
                                                              └─────────────────┘
```

### 3.2 分层职责

| 层级 | 目录 | 职责 |
|------|------|------|
| **路由层** | `api/routes/` | HTTP 协议处理，参数校验，权限拦截 |
| **服务层** | `services/` | 业务编排（RAG 流程、文档入库流程） |
| **核心层** | `core/` | 单一职责的引擎（解析、分块、检索、生成） |
| **容器层** | `container.py` | DI 容器，统一服务生命周期 |
| **基础设施** | `config.py` / `logger.py` | 配置加载、结构化日志、Trace ID |

### 3.3 关键数据流

**文档异步入库（推荐生产路径）**：

```
POST /api/upload/async
   ↓
保存临时文件 + 返回 task_id (毫秒级响应)
   ↓
Celery 队列 doc_process
   ↓
process_document 任务
   ├─ Stage 1: 解析（unstructured / PyMuPDF / Baichuan fallback）
   ├─ Stage 2: 分块（7 种策略可选）
   ├─ Stage 3: Embedding（bge-m3 批量）
   ├─ Stage 4: 写入 Milvus
   ├─ Stage 5: 元数据写入 PostgreSQL
   ├─ Stage 6: 更新 BM25 索引
   └─ Stage 7: 清理临时文件
   ↓
update_state(PROGRESS) 实时进度
   ↓
前端轮询 GET /api/tasks/{task_id}
```

**问答查询流程（带意图判断）**：

```
用户提问
   ↓
意图判断（关键词快速路径 / LLM 判断）
   ├─ chat：直接调 LLM 回答（不检索）
   └─ retrieve：进入完整 RAG 流程
       ↓
   查询改写（生成 3 个改写 query）
       ↓
   权限过滤（注入 doc_id in [...]）
       ↓
   并行混合检索
       ├─ 向量检索（Milvus + bge-m3）
       └─ 关键词检索（jieba + BM25）
       ↓
   RRF 融合去重
       ↓
   BGE Reranker 重排序（取 top_k_rerank）
       ↓
   构建上下文 Prompt + 会话历史（最近 5 轮）
       ↓
   LLM 流式生成（SSE）
       ↓
   异步保存对话历史
```

---

## 四、核心模块实现

### 4.1 依赖注入容器

**为什么需要 DI 容器？**

传统的全局单例（`_milvus_store = None` + `get_milvus_store()`）会让单元测试痛苦：mock 一个 Milvus 客户端要 monkey-patch 整个模块。本项目用 `ServiceContainer` 解决：

```python
# backend/app/container.py
from app.container import container, get_milvus_store

# 注册（启动时一次）
container.register(MilvusStore, MilvusStore, singleton=True)

# 路由中使用
@router.get("/docs")
async def list_docs(store: MilvusStore = Depends(get_milvus_store)):
    ...

# 测试覆盖
container.override(MilvusStore, MockMilvusStore())
```

**关键设计**：
- 延迟注册（`_lazy_register_all`）避免循环导入
- 单例缓存 + 测试覆盖优先级清晰
- 应用关闭时 `container.close_all()` 统一释放资源（连接池、模型权重）

### 4.2 七种分块策略

`backend/app/core/chunking.py` 实现 Dify 风格的分块引擎：

| 策略 | 适用场景 | 关键参数 |
|------|---------|---------|
| `auto` | 通用，自动按文本特征选择 | - |
| `fixed_size` | 结构化数据、表格 | `chunk_size`, `chunk_overlap`, `separator` |
| `recursive` | 通用长文本（段落→句子→词→字符） | `chunk_size`, `chunk_overlap` |
| `semantic` | 强主题切换的长文档 | `similarity_threshold` |
| `structured` | 有清晰标题层级的 MD/HTML | 按 H1/H2/H3 切分 |
| `parent_child` | 长文档需要"小检索大上下文" | `parent_chunk_size`, `child_chunk_size` |
| `llm_smart` | 极重要文档，预算充足 | 调用 LLM 生成切分点 |

**关键经验**：
- 默认用 `recursive`，`chunk_size=512`, `overlap=128` 已能覆盖 80% 场景
- `parent_child` 在长 PDF（如制度汇编）效果显著：用 256 字的子块检索，返回 2048 字的父块给 LLM
- `llm_smart` 慎用，每次切分都要烧 token，仅推荐用于核心知识文档

### 4.3 混合检索 + RRF 融合

`backend/app/core/retrieval.py` 实现"向量 + BM25"双路召回：

```python
# 简化版伪代码
async def retrieve(query, top_k=10):
    # 双路并行
    vec_results, bm25_results = await asyncio.gather(
        vector_search(query, top_k=10),    # Milvus 语义相似
        bm25_search(query, top_k=10),      # jieba + BM25 关键词
    )

    # RRF 融合（k=60 为经验值）
    fused = rrf_fusion(vec_results, bm25_results, k=60)
    return fused[:top_k]
```

**BM25 索引存放策略**：

| 方案 | 优点 | 缺点 | 项目选择 |
|------|------|------|---------|
| 内存索引 | 查询快 | 重启丢失，多实例不一致 | 当前方案 |
| Milvus Sparse Vector | 与向量同源 | 2.4+ 才稳定 | 未来迁移 |
| Elasticsearch | 全文检索强 | 引入额外组件 | 不采用 |

**优化点**：项目通过 `HybridRetriever` 启动时从 Milvus 全量加载重建，并提供 `/admin/rebuild-bm25` 热重建端点。Celery 也有定时维护任务 `rebuild_bm25_index`。

### 4.4 异步文档处理

**为什么不能用 FastAPI 的 `BackgroundTasks`？**
- `BackgroundTasks` 跑在同一进程，长任务阻塞 Worker
- 不支持重试、进度回调、跨实例
- 进程崩溃任务丢失

**Celery 方案**：

```python
# backend/app/core/tasks.py
@celery_app.task(
    bind=True,
    max_retries=3,
    default_retry_delay=60,
    queue="doc_process",
)
def process_document(self, file_path, filename, ...):
    self.update_state(state="PROGRESS", meta={"stage": "parsing", "progress": 10})
    # ... 解析 / 分块 / 入库
    return {"doc_id": ..., "status": "success"}
```

**启动 Worker**：

```bash
celery -A app.core.celery_app worker \
    --loglevel=info \
    --queues=celery,doc_process,maintenance
```

**前端轮询进度**：

```javascript
const pollTask = async (taskId) => {
  while (true) {
    const { state, meta } = await api.get(`/api/tasks/${taskId}`)
    updateProgress(meta.progress, meta.message)
    if (state === 'SUCCESS' || state === 'FAILURE') break
    await sleep(1000)
  }
}
```

### 4.5 意图判断 + 多轮会话

**意图判断**：避免简单"你好"也走完整 RAG 流程：

```python
# 规则匹配（快速路径，不走 LLM）
CHITCHAT_KEYWORDS = {"你好", "谢谢", "你是谁", "再见", ...}

if any(kw in question.lower() for kw in CHITCHAT_KEYWORDS):
    intent = "chat"  # 直接走闲聊 prompt
else:
    intent = await llm_classify(question)  # LLM 兜底
```

**多轮会话**：通过 `conversation_id` 关联：

```python
# 加载最近 5 轮历史
history = await metadata_store.get_messages(
    conversation_id, limit=10
)
# 注入到 LLM messages
messages = [system_prompt, *history, user_question]
```

---

## 五、开发规范与工程实践

### 5.1 后端开发规范

**异步优先**：

```python
# 正确：使用 async/await + Depends 注入
@router.get("/documents")
async def list_documents(
    store: MetadataStore = Depends(get_metadata_store),
    current_user: dict = Depends(get_current_user),
):
    return await store.list_documents()
```

**Pydantic 严格校验**：

```python
class QueryRequest(BaseModel):
    question: str = Field(..., min_length=1, max_length=1000)
    top_k: int = Field(default=5, ge=1, le=20)
    conversation_id: Optional[str] = None
    use_rerank: bool = True
    stream: bool = False
```

**配置三层优先级**：

```python
# backend/app/config.py
# .env 文件 > 环境变量 > 代码默认值
class Settings(BaseSettings):
    SECRET_KEY: str = "change-me"  # 启动时强制校验长度 >= 32
    METADATA_DB_URL: str = "sqlite+aiosqlite:///./metadata.db"
```

### 5.2 前端开发规范

**API 拦截器统一处理 JWT**：

```javascript
// frontend/src/api/api.js
axios.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

axios.interceptors.response.use(
  res => res,
  err => {
    if (err.response?.status === 401) router.push('/login')
    return Promise.reject(err)
  }
)
```

**状态持久化**：

```javascript
const currentConversationId = ref('')
watch(currentConversationId, (val) => {
  localStorage.setItem('rag_conv_id', val)
})
```

### 5.3 结构化日志（带 Trace ID）

```python
# backend/app/logger.py
logger = get_logger(__name__)
logger.info("文档处理完成", extra={
    "doc_id": doc_id,
    "chunk_count": len(chunks),
    "department": department,
    "trace_id": get_trace_id(),  # 自动从 LoggingMiddleware 注入
})
```

输出（JSON 行格式）：
```json
{"timestamp":"2026-05-15T10:23:11Z","level":"INFO","trace_id":"a3f1b2","msg":"文档处理完成","doc_id":"...","chunk_count":42}
```

### 5.4 代码组织约定

```
backend/app/
├── api/routes/          # 路由：1 文件 1 资源，无业务逻辑
├── services/            # 服务：业务编排，调用多个 core
├── core/                # 核心：单一职责的引擎
├── models/schemas.py    # Pydantic schemas（请求/响应）
├── container.py         # DI 容器
├── config.py            # Pydantic Settings
├── logger.py            # 日志 + Trace
└── main.py              # FastAPI 入口
```

**禁止**：
- 路由层直接调 `core/`，必须通过 `services/`
- 在 `core/` 模块内 import `services/`
- 跨模块的全局变量

---

## 六、权限与安全

### 6.1 三角色权限模型

| 角色 | 文档查看 | 文档上传 | 文档删除 | 用户管理 |
|------|---------|---------|---------|---------|
| Admin | 全部 | 任意部门 | 全部 | 支持 |
| Staff | 公共 + 本部门 | 仅本部门 | 本部门 | 不支持 |
| User | 仅公共 | 不支持 | 不支持 | 不支持 |

### 6.2 检索层权限过滤（核心）

```python
async def _inject_doc_permission_filter(request, current_user, store):
    role = current_user.get("role", "user")
    if role == "admin":
        return  # 管理员不过滤

    public_ids = set(await store.get_public_doc_permissions())
    if role == "staff":
        dept_docs = await store.list_documents_by_department(
            current_user.get("department")
        )
        public_ids.update(d["doc_id"] for d in dept_docs)

    # 注入到 Milvus 过滤表达式
    request.filters["doc_id"] = list(public_ids)
```

**为什么必须在检索层过滤？**
- 应用层过滤会让向量检索返回无权限的结果，相当于泄露语义信息
- Milvus 的 `doc_id in [...]` 表达式可以走索引，性能开销极小
- 双重校验：检索层 + API 层两道闸门

### 6.3 JWT 安全要点

```python
# backend/app/core/security.py
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

def create_access_token(data: dict, expires_delta: timedelta = timedelta(days=7)):
    to_encode = data.copy()
    to_encode.update({"exp": datetime.utcnow() + expires_delta})
    return jwt.encode(to_encode, settings.SECRET_KEY, algorithm="HS256")
```

**部署陷阱**：
- `bcrypt>=5.0` 引入了 72 字节密码长度硬限制，会破坏 `passlib`。**必须锁定 `bcrypt<5.0`**。
- `SECRET_KEY` 长度 < 32 时，应用启动会打印警告。生产环境必须改。
- Token 过期时间建议 7 天，配合前端 `localStorage` + 401 自动跳转登录。

### 6.4 CORS 与生产加固

```python
# 开发环境
app.add_middleware(CORSMiddleware, allow_origins=["*"])

# 生产环境（必改）
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://your-domain.com"],
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
)
```

---

## 七、可观测性与运维

### 7.1 三大支柱

| 支柱 | 实现 | 端点/工具 |
|------|------|----------|
| **Logs** | 结构化 JSON + Trace ID | `backend/logs/*.log` |
| **Metrics** | Prometheus | `GET /metrics` |
| **Traces** | Trace ID 贯穿请求 | 日志关联 |

### 7.2 Prometheus 指标

项目内置 `/metrics` 端点，记录：

```python
# backend/app/core/metrics.py
http_requests_total{method, path, status}
http_request_duration_seconds{method, path}
celery_tasks_total{task_name, state}
rag_query_duration_seconds{stage}  # parse/retrieve/rerank/llm
```

接入 Grafana 后建议看板：
- **请求维度**：QPS、p50/p95/p99 延迟、错误率
- **RAG 维度**：检索召回数、重排耗时、LLM 首字节延迟
- **Celery 维度**：队列堆积、任务成功率、平均处理时长

### 7.3 健康检查

`GET /api/health` 优雅降级：

```json
{
  "status": "degraded",
  "components": {
    "milvus": {"status": "ok", "latency_ms": 5},
    "ollama": {"status": "ok", "models": ["bge-m3", "qwen2.5"]},
    "postgres": {"status": "ok"},
    "redis": {"status": "down", "error": "Connection refused"}
  }
}
```

外部依赖挂掉只标记 `degraded`，不让健康检查直接 500，避免被 K8s 误杀。

---

## 八、部署方案

### 8.1 Docker Compose 一键部署

```yaml
# docker-compose.yml（精简版）
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: rag
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: rag_metadata
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rag"]

  redis:
    image: redis:7-alpine
    volumes:
      - redis-data:/data

  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - METADATA_DB_URL=postgresql+asyncpg://rag:${POSTGRES_PASSWORD}@db:5432/rag_metadata
      - REDIS_URL=redis://redis:6379/0
    env_file: .env
    depends_on:
      db: {condition: service_healthy}
      redis: {condition: service_started}

  celery-worker:
    build: ./backend
    command: celery -A app.core.celery_app worker --loglevel=info --queues=celery,doc_process,maintenance
    env_file: .env
    depends_on: [db, redis]

volumes:
  postgres-data:
  redis-data:
```

**Milvus 与 Ollama 单独部署**（避免 compose 文件过大）：

```bash
# Milvus
wget https://github.com/milvus-io/milvus/releases/download/v2.4.0/milvus-standalone-docker-compose.yml
docker compose -f milvus-standalone-docker-compose.yml up -d

# Ollama（建议直接装宿主机以共享 GPU）
curl -fsSL https://ollama.com/install.sh | sh
ollama pull bge-m3
ollama pull qwen2.5:7b-instruct
```

### 8.2 环境变量清单

`.env` 文件（项目根目录）：

```bash
# 安全（必改）
SECRET_KEY=use-openssl-rand-hex-32-to-generate

# Milvus
MILVUS_HOST=192.168.1.100
MILVUS_PORT=19530
MILVUS_COLLECTION_NAME=enterprise_docs

# Ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_EMBED_MODEL=bge-m3
OLLAMA_LLM_MODEL=qwen2.5:7b-instruct

# 元数据库
METADATA_DB_URL=postgresql+asyncpg://rag:password@db:5432/rag_metadata

# Celery
REDIS_URL=redis://redis:6379/0

# 分块默认值
CHUNK_SIZE=512
CHUNK_OVERLAP=128
DEFAULT_CHUNK_STRATEGY=auto

# 检索
TOP_K_VECTOR=10
TOP_K_BM25=10
TOP_K_RERANK=5
```

### 8.3 部署前检查清单

| 检查项 | 命令 / 说明 |
|--------|-------------|
| `bcrypt<5.0` | `pip install "bcrypt<5.0"` |
| `SECRET_KEY` | `openssl rand -hex 32` 生成，长度 >= 32 |
| Milvus 连通 | `python test_milvus_connection.py` |
| Ollama 模型 | `ollama list` 确认 `bge-m3`、`qwen2.5:7b-instruct` 已下载 |
| PostgreSQL | `python test_pgsql_connection.py` |
| Redis | `redis-cli ping` 返回 `PONG` |
| 显存 | 7B INT4 量化约需 6-8GB |
| GPU 推理 | `nvidia-smi` 确认驱动 + CUDA |

### 8.4 从 SQLite 平滑迁移到 PostgreSQL

```bash
# 1. 启动 PostgreSQL
docker compose up -d db

# 2. 修改 .env
# METADATA_DB_URL=postgresql+asyncpg://rag:password@localhost:5432/rag_metadata

# 3. 重启后端，SQLAlchemy 会自动建表
# 4. 历史数据迁移（可选，写个一次性脚本即可）
python scripts/migrate_sqlite_to_postgres.py
```

---

## 九、常见问题与排障

### 9.1 问答无返回内容

**现象**：AI 气泡空白，一直转圈。

**排查清单**：
1. Ollama 是否运行：`curl http://localhost:11434/api/tags`
2. 模型是否加载：`ollama list` 应有 `bge-m3`、`qwen2.5:7b-instruct`
3. 后端日志看 `trace_id` 关联请求：`grep "trace_id=xxx" backend/logs/*.log`
4. `REWRITE_PROMPT` 中的 `{}` 是否转义为 `{{}}`（Python f-string 陷阱）
5. SSE 流式请求是否带 Token：`Authorization: Bearer xxx`

### 9.2 切换页面消息丢失

**现象**：切到文档上传再回来，对话历史清空。

**排查清单**：
1. `localStorage` 是否保存了 `currentConversationId`
2. `onMounted` 是否调用了 `loadConversation(id)`
3. `switchConversation` 是否有重复检查导致直接 return
4. 后端 `query_stream` 是否用 `BackgroundTasks` 保存消息（不能阻塞流式响应）

### 9.3 异步任务一直 PENDING

**现象**：上传后 `task_id` 状态永远 PENDING。

**排查清单**：
1. Celery Worker 是否启动：`ps aux | grep celery`
2. Worker 是否监听了对应队列：`--queues=celery,doc_process,maintenance`
3. Redis 连接：`redis-cli -h localhost ping`
4. 任务名是否被正确注册：`celery -A app.core.celery_app inspect registered`

### 9.4 权限管理页面加载失败

**现象**：用户列表空白，公共权限加载失败。

**排查清单**：
1. 后端是否运行（端口冲突检查 `lsof -i:8000`）
2. `listUsers()` / `listPublicDocuments()` 是否返回 200
3. `DocumentInfo` schema 是否包含 `department` 字段
4. `document_service.list_documents()` 是否从元数据库读取（而非 Milvus）

### 9.5 Windows 端口僵尸问题

**现象**：端口 8000 显示 LISTENING，但找不到进程。

**解决**：
```bash
# 换端口启动
uvicorn app.main:app --port 8001 --reload

# 前端 vite.config.ts 代理同步修改
proxy: { '/api': { target: 'http://localhost:8001' } }
```

### 9.6 检索结果不相关

**排查清单**：
1. 分块策略是否合适（长文档优先 `parent_child` 或 `structured`）
2. `chunk_size` 是否过大（建议 300-800 字符）
3. `overlap` 是否足够（建议 50-150 字符）
4. 是否启用了**查询改写**和**多查询并行检索**
5. Embedding 模型是否匹配语言（`bge-m3` 中英混合、`bge-large-zh` 纯中文）
6. 用 Attu 直接看 Milvus 数据是否真的入库了

---

## 十、性能调优

### 10.1 检索速度优化

| 优化手段 | 效果 | 实施难度 |
|---------|------|---------|
| Milvus IVF_FLAT → HNSW | 查询 100ms → 10ms | 低 |
| Embedding GPU 推理 | 单条 200ms → 20ms | 中（需 CUDA） |
| 多查询并行 (`asyncio.gather`) | 串行 3s → 并行 1s | 低 |
| 查询缓存（Redis） | 热门查询 0ms | 中 |
| BM25 全量预加载 | 启动后零延迟 | 低（已实现） |

### 10.2 LLM 生成速度优化

| 优化手段 | 效果 |
|---------|------|
| INT4 量化 | 显存减半，速度略降 |
| vLLM 替代 Ollama | 批量推理，吞吐 10x（设置 `LLM_PROVIDER=vllm`） |
| 减少 `top_k_rerank` | 上下文短，生成快（建议 3-5） |
| 流式输出 | 首字节 < 1s，用户感知大幅改善 |
| 意图判断快速路径 | 闲聊直接 LLM，省检索 |

### 10.3 并发处理

```python
# 多查询并行检索
import asyncio

queries = await rewrite_query(question)  # 生成 3 个改写
results = await asyncio.gather(*[
    retriever.retrieve(q) for q in queries
])
fused = rrf_fusion(*results)
```

### 10.4 索引参数调优

```python
# milvus_store.py 建索引
index_params = {
    "index_type": "IVF_FLAT",   # 数据量 < 100w
    # "index_type": "HNSW",     # 数据量 > 100w，召回更稳
    "metric_type": "COSINE",
    "params": {"nlist": 1024},
}

# 检索参数
search_params = {
    "metric_type": "COSINE",
    "params": {"nprobe": 16},   # IVF 经验值：sqrt(nlist)
    # "params": {"ef": 64},     # HNSW
}
```

### 10.5 资源水位监控

```bash
# 一行命令看 Worker 队列堆积
celery -A app.core.celery_app inspect active_queues

# Milvus 集合状态
attu  # 浏览器打开 http://localhost:3000

# Ollama GPU 占用
nvidia-smi --query-gpu=utilization.gpu,memory.used --format=csv -l 1
```

---

## 十一、上线检查清单

### 11.1 功能验收

- [ ] **模型就绪**：`bge-m3` + `qwen2.5:7b-instruct` 已下载并测试
- [ ] **向量库就绪**：Milvus collection 已创建，索引已构建
- [ ] **元数据库就绪**：PostgreSQL 连接通，表结构已创建
- [ ] **Celery Worker**：监听 `doc_process` + `maintenance` 队列
- [ ] **权限模型**：Admin/Staff/User 三角色测试通过
- [ ] **文档上传**：PDF/Word/Excel/PPT/MD/HTML 解析测试通过
- [ ] **异步上传**：100MB 大文件可成功上传并返回 task_id
- [ ] **问答测试**：流式 + 非流式均正常，引用溯源正确
- [ ] **意图判断**：闲聊问题不走检索，直接回复
- [ ] **多轮会话**：切换页面、刷新后对话历史保留

### 11.2 安全加固

- [ ] **`SECRET_KEY`**：长度 >= 32，已用 `openssl rand` 生成
- [ ] **bcrypt 版本**：锁定 < 5.0
- [ ] **CORS**：生产环境限制 `allow_origins`
- [ ] **JWT 过期**：7 天合理，敏感场景可缩短
- [ ] **密码哈希**：bcrypt + 加盐
- [ ] **HTTPS**：Nginx 配置 TLS 证书
- [ ] **数据库密码**：不在代码中硬编码，通过 `.env` 或 Vault

### 11.3 可观测性

- [ ] **日志完备**：关键操作均有结构化日志（含 trace_id）
- [ ] **Prometheus 抓取**：`/metrics` 端点接入 Grafana
- [ ] **健康检查**：`/api/health` 接入负载均衡探针
- [ ] **告警规则**：QPS、延迟、错误率、队列堆积阈值

### 11.4 备份与容灾

- [ ] **PostgreSQL 备份**：每日 `pg_dump` + 异地存储
- [ ] **Milvus 快照**：定期 backup，可用 `milvus-backup` 工具
- [ ] **Celery 任务幂等**：失败重试不会重复写入向量库
- [ ] **优雅停机**：Worker 处理完当前任务再退出

---

## 十二、总结与延伸

### 12.1 五条核心经验

企业级 RAG 落地不是"把文档扔给向量库就能跑"的简单事。它是一套系统工程：

1. **文档质量决定上限**：再好的检索也救不了烂文档。建议先用 10-20 篇核心文档跑通效果，再批量入库。
2. **权限隔离是底线**：企业数据绝不能裸奔。检索层过滤是关键，应用层过滤只是兜底。
3. **流式体验是标配**：用户不会等 30 秒才看到第一个字。意图判断 + SSE 是首字节延迟的关键。
4. **可观测性是保障**：没有日志和监控，出了问题只能靠猜。Trace ID 贯穿请求 + Prometheus 指标缺一不可。
5. **异步处理是规模化前提**：大文档不能阻塞 API。Celery + Redis 让 100MB PDF 上传从"5 分钟超时"变成"毫秒级响应 + 后台处理"。

### 12.2 落地路径建议

**第一阶段（1-2 周）：MVP 验证**
- 一个部门 + 50 篇文档跑通全流程
- SQLite 元数据，无 Celery，同步处理足够
- 验证检索效果和用户接受度

**第二阶段（2-4 周）：工程化加固**
- 切 PostgreSQL，引入 Celery
- 上 Docker Compose，加 Prometheus
- 完善权限模型和日志

**第三阶段（持续迭代）：效果优化**
- 调分块策略和检索参数
- 调 Prompt 工程
- 评估是否引入 vLLM 提升吞吐
- 监控用户反馈，建立评估集

### 12.3 进阶方向

| 方向 | 关键词 | 收益 |
|------|--------|------|
| 多模态 RAG | 图片、表格理解 | PDF 中图表能被检索 |
| Agent 化 | 工具调用、规划 | 从"问答"升级到"任务执行" |
| 知识图谱融合 | GraphRAG | 结构化知识 + 非结构化检索 |
| 增量索引 | 文档版本管理 | 支持文档更新而不重建 |
| 评估闭环 | RAGAS、人工反馈 | 持续衡量和改进效果 |

### 12.4 项目仓库

本指南配套开源项目：**Enterprise RAG Agent**
- 后端：FastAPI + Pydantic + SQLAlchemy 2.0
- 前端：Vue 3 + Element Plus + Vite
- 完整实现：7 种分块、混合检索、Celery 异步、三角色权限、多轮会话

**最核心的建议**：先用一个小场景（比如一个部门的产品手册）跑通全流程，验证效果后再逐步扩展到全公司。**不要一上来就追求"大而全"，让架构和团队一起进化**。
